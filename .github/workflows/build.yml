name: Build (Kiran Flake)

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    with:
      archive-artifact-name-suffix: ""
    secrets: inherit
